<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 位置情報パーミッション -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- バイブレーション（達成演出用） -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 画面ロック中のGPS継続取得用 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/sd_512"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sd_512"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShigaDevelopment"
        tools:targetApi="31">

        <!-- Google Maps APIキー -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- メニュー画面（ランチャー起点） -->
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ストーリープロローグ画面（探索モード初回起動時） -->
        <activity
            android:name=".StoryPrologueActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />

        <!-- ストーリーエピローグ（エンディング）画面（全市町制覇時） -->
        <activity
            android:name=".StoryEpilogueActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />

        <!-- ヘルプ画面 -->
        <activity
            android:name=".HelpActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 探索（地図ゲーム）画面 -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>

</manifest>
